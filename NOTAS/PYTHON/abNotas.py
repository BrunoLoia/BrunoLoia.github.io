# UNPLASH = DESCARGAR IMG
# BOXICONS = LOGOS(SVG)
# NORMALIZE = IGUAL EN TODOS LOS NAVEGADORES
# figma = editor grafico 
# (.venv) C:\Users\Usuario\Desktop\python-website\myproject>flask --app index --debug run
# .venv\Scripts\activate
# code.
# clippy css = formas de imagenes
# magicpattern = dise;os 
# formspree = permitir enviar correos sin usar un lenguaje del backend o que se a del lado del servidor, sino con html
#meta tags = generador de etiquetas 